var Config = (function() {
  function Config() {
    this.goodies = goodies;

    this.levels = {
      1: levelOne
    };

    this.colors = {
      red: '',
      green: '',
      blue: ''
    };
  }

  return Config;
})();
